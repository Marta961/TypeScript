# Laboratorio 1 — Estadística y casos límite
## Funciones

- `calcularMedia`: me devuelve `number | null` si el array está vacío.
- `calcularMediana`: es el idem.
- `filtrarAtipicos`: elimina valores fuera del `limite` y con σ = 0 no se elimina ningún valor para poder evitar vaciar el conjunto por equivocación.

## Ejecución
Desde `modulo-1/`:
```bash
npx tsx src/index.ts
npx tsc
```